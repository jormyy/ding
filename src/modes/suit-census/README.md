# Suit Census

Suit Census is a high-hand information variant. The table sees remaining-deck suit counts.

## Engine Notes

- Uses standard dealing and high-hand scoring.
- Uses `modeInfo` with `suit-census` to expose remaining H/D/C/S counts.
- The info payload is public and does not change scoring.
