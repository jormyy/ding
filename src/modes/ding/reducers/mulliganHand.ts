/**
 * `mulliganHand` reducer — owner spends a one-time full-hand redraw during
 * deal-choice modes that allow mulligans.
 */
export { mulliganHand as reduceMulliganHand } from "../../../../party/handlers/dealChoice";
