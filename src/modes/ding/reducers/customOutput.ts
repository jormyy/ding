/**
 * `customOutput` reducer — broadcast a custom-output event for clients with
 * the `-=` name-prefix marker; relayed via raw broadcast (no state change).
 */
export { customOutput as reduceCustomOutput } from "../../../../party/handlers/social";
