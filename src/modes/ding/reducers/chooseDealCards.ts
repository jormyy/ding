/**
 * `chooseDealCards` reducer — owner locks the cards they keep during an
 * explicit deal-choice phase.
 */
export { chooseDealCards as reduceChooseDealCards } from "../../../../party/handlers/dealChoice";
