/**
 * `fuckoff` reducer — append a fuckoff event to the social log and broadcast
 * a raw "fuckoff" message so all (non-actor) clients play the sound.
 */
export { fuckoff as reduceFuckoff } from "../../../../party/handlers/social";
