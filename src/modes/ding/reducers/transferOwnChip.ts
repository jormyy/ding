/**
 * `transferOwnChip` reducer — move the chip from one of the player's own
 * hands to another (where the target is currently unranked).
 */
export { transferOwnChip as reduceTransferOwnChip } from "../../../../party/handlers/ranking";
