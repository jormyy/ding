/**
 * `ready` reducer — toggle the player's ready flag. When all connected
 * players are ready, the lifecycle handler advances the phase.
 */
export { ready as reduceReady } from "../../../../party/handlers/lifecycle";
