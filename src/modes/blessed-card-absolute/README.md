# Blessed Card Absolute

Blessed Card Absolute is a high-hand hidden-absolute variant. A specific card forces its holder to true-rank first.

## Engine Notes

- Uses the blessed-card deck marker.
- Uses `forceRankByMeta.first = "blessed"`.
- Distinct id avoids colliding with the earlier `blessed-card` catalogue entry.
