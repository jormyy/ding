# Rank Census

Rank Census is a high-hand information variant. The table sees remaining-deck rank counts.

## Engine Notes

- Uses standard dealing and high-hand scoring.
- Uses `modeInfo` with `rank-census` to expose remaining rank composition.
- The info payload is public and symmetric.
