# Wild Connector

Wild Connector is a high-hand rank-identity variant. Adjacent ranks can behave like a pair.

## Engine Notes

- Uses standard dealing.
- Uses `syntheticPair: "adjacent"` to append a synthetic same-rank card when adjacent ranks are present.
- This preserves cooperative ranking while making connectors materially stronger at reveal.
