# Mirror Hole

Mirror Hole is a high-hand neighbor-information variant. The accelerated cooperative version surfaces a public mirror notice while keeping the base private-hand flow intact.

## Engine Notes

- Uses standard dealing and high-hand scoring.
- Uses `modeInfo` to expose the neighbor-mirror rule symmetrically.
- Exact per-viewer "left neighbor only" masking remains a client/display-layer follow-up.
