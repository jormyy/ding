# Sample Draw

Sample Draw is a high-hand deck-information variant. Three off-board deck samples are shown at river.

## Engine Notes

- Uses standard dealing and high-hand scoring.
- Uses `modeInfo` only during river to expose three non-scoring cards from `dealDeck`.
- The sampled cards do not alter community cards or showdown.
