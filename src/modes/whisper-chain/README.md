# Whisper Chain

Whisper Chain is a high-hand hole-information variant. Each left neighbor knows one of your hole cards.

## Engine Notes

- Uses standard dealing and high-hand scoring.
- Uses one public hole-card hint across preflop/flop/turn/river as the accelerated symmetric representation.
- Uses `modeInfo` with `whisper-chain` to explain the rule on the wire.
