# Decoy

Decoy is a high-hand information variant. A face-down decoy exists on the table but does not score.

## Engine Notes

- Uses standard dealing and high-hand scoring.
- Uses `modeInfo` with `decoy` to announce the non-scoring decoy.
- A visible decoy card slot remains a client-layer follow-up.
