# Rank Whisper

Rank Whisper is a high-hand hint variant. The table receives a true rank-absence hint.

## Engine Notes

- Uses standard dealing and high-hand scoring.
- Uses `modeInfo` to publish a deterministic rank that one hand does not hold.
- Exact per-player private whisper delivery remains a display-layer follow-up.
