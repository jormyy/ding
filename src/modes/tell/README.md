# Tell

Tell is a high-hand truth-information variant. Each phase provides one true public fact.

## Engine Notes

- Uses standard dealing and high-hand scoring.
- Uses `modeInfo` to publish a deterministic true fact about the board when available, otherwise deck count.
- The fact is public and symmetric.
