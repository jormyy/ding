# Phantom Card

Phantom Card is a high-hand hint variant. The table is told a rank that is absent from at least one hand.

## Engine Notes

- Uses standard dealing and high-hand scoring.
- Uses `modeInfo` to derive a phantom rank from remaining deck state.
- The hint never changes scoring or card identities.
